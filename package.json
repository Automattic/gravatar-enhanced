{
	"name": "gravatar-enhanced",
	"version": "0.3.0",
	"description": "Gravatar Enhanced WordPress plugin",
	"main": "index.js",
	"scripts": {
		"start": "echo 'Building Gravatar Enhanced...'",
		"build": "echo 'Building Gravatar Enhanced...'",
		"release": "yarn build && rm -rf release && mkdir -p release && cp -R *.php readme.txt *.png release",
		"dist": "yarn release && rm -rf dist && mkdir dist && mv release gravatar-enhanced && zip gravatar-enhanced.zip -r gravatar-enhanced && mv gravatar-enhanced release && mv gravatar-enhanced.zip dist && release-it",
		"dist:svn": "./dev/svn.sh $npm_package_version"

	},
	"author": "",
	"license": "GPL-2.0-only",
	"release-it": {
		"github": {
			"release": true,
			"assets": [
				"dist/gravatar-enhanced.zip"
			]
		},
		"npm": false
	},
	"devDependencies": {
		"release-it": "^17.6.0"
	}
}
